该案例中的问题是，存在一个公有变量的 py 文件，变量的值为不可变类型。
如果使用 import 导入这个公有变量 py 文件的值到程序中，那么无论被多少个程序导入，指向的都是该被导入的模块，是一个全局变量
但是如果是使用form common import xxx 的话，那么在导入的程序中，是在这个程序新建了一个变量 xxx 指向了原本模块中 xxx 的值。如果是在一个程序中某个函数调用了这个 xxx ，那么 xxx 只是这个函数的一个局部变量，即使用了 global 作为了全局变量，但因为 xxx 只是改程序的变量，对于它的修改只会在这个程序中生效，不会修改原本的模块中的 xxx 的值

但是如果公有变量的 py 文件的变量值为可变类型，那么无论 import 还是 form xxx import 都可以对模块中的值进行修改
